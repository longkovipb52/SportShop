@model SportShop.Models.Brand

@{
    ViewData["Title"] = $"Chỉnh sửa thương hiệu - {Model.Name}";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/admin_brands.css" asp-append-version="true" />
}

<div class="brand-form-container">
    <!-- Header -->
    <div class="page-header">
        <div class="header-content">
            <div class="header-right">
                <a href="@Url.Action("Details", new { id = Model.BrandID })" class="btn btn-info">
                    <i class="fas fa-eye"></i>
                    Xem chi tiết
                </a>
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left"></i>
                    Quay lại
                </a>
            </div>
        </div>
    </div>

    <!-- Thông báo -->
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-triangle"></i>
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Form -->
    <div class="form-section">
        <div class="row">
            <div class="col-lg-8">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title">
                            <i class="fas fa-info-circle"></i>
                            Thông tin thương hiệu
                        </h5>
                    </div>
                    <div class="card-body">
                        <form asp-action="Edit" method="post" enctype="multipart/form-data">
                            <input type="hidden" asp-for="BrandID" />
                            <input type="hidden" asp-for="Logo" />
                            <input type="hidden" asp-for="CreatedAt" />
                            
                            <div asp-validation-summary="ModelOnly" class="text-danger validation-summary"></div>
                            
                            <div class="mb-3">
                                <label asp-for="Name" class="form-label required">Tên thương hiệu</label>
                                <input asp-for="Name" class="form-control" placeholder="Nhập tên thương hiệu...">
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <label asp-for="Description" class="form-label">Mô tả</label>
                                <textarea asp-for="Description" class="form-control" rows="4" 
                                         placeholder="Nhập mô tả cho thương hiệu..."></textarea>
                                <span asp-validation-for="Description" class="text-danger"></span>
                                <div class="form-text">Mô tả chi tiết về thương hiệu này</div>
                            </div>

                            <!-- Logo Upload Section -->
                            <div class="mb-3">
                                <label class="form-label">Logo thương hiệu</label>
                                <div class="logo-upload-section">
                                    <div class="logo-preview-container">
                                        <div class="logo-preview" id="logoPreview">
                                            @if (!string.IsNullOrEmpty(Model.Logo))
                                            {
                                                <img src="~/upload/branch/@Model.Logo" alt="@Model.Name" class="current-logo">
                                                <div class="logo-overlay">
                                                    <button type="button" class="btn btn-sm btn-outline-light" onclick="changeLogo()">
                                                        <i class="fas fa-camera"></i>
                                                        Thay đổi
                                                    </button>
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="preview-placeholder">
                                                    <i class="fas fa-image"></i>
                                                    <p>Chưa có logo</p>
                                                </div>
                                            }
                                        </div>
                                        <div class="logo-actions" id="logoActions" style="display: none;">
                                            <button type="button" class="btn btn-sm btn-success" onclick="confirmLogo()">
                                                <i class="fas fa-check"></i>
                                                Xác nhận
                                            </button>
                                            <button type="button" class="btn btn-sm btn-danger" onclick="cancelLogo()">
                                                <i class="fas fa-times"></i>
                                                Hủy
                                            </button>
                                        </div>
                                    </div>
                                    
                                    <div class="logo-upload-controls">
                                        <input type="file" id="logoFile" name="logoFile" accept="image/*" 
                                               class="form-control" onchange="previewLogo(this)" style="display: none;">
                                        <button type="button" class="btn btn-outline-primary" onclick="document.getElementById('logoFile').click()">
                                            <i class="fas fa-upload"></i>
                                            @(string.IsNullOrEmpty(Model.Logo) ? "Tải lên logo" : "Thay đổi logo")
                                        </button>
                                        <div class="form-text">
                                            <i class="fas fa-info-circle"></i>
                                            Chọn logo (JPG, PNG, GIF) - Tối đa 5MB
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="form-actions">
                                <button type="submit" class="btn btn-success">
                                    <i class="fas fa-save"></i>
                                    Cập nhật thương hiệu
                                </button>
                                <a href="@Url.Action("Index")" class="btn btn-secondary">
                                    <i class="fas fa-times"></i>
                                    Hủy
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <!-- Card thống kê -->
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title">
                            <i class="fas fa-chart-bar"></i>
                            Thống kê thương hiệu
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="stats-grid" id="brandStats">
                            <div class="stat-item">
                                <div class="stat-icon">
                                    <i class="fas fa-box text-primary"></i>
                                </div>
                                <div class="stat-content">
                                    <div class="stat-number" id="totalProducts">
                                        <i class="fas fa-spinner fa-spin"></i>
                                    </div>
                                    <div class="stat-label">Tổng sản phẩm</div>
                                </div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-icon">
                                    <i class="fas fa-warehouse text-success"></i>
                                </div>
                                <div class="stat-content">
                                    <div class="stat-number" id="totalStock">
                                        <i class="fas fa-spinner fa-spin"></i>
                                    </div>
                                    <div class="stat-label">Tổng tồn kho</div>
                                </div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-icon">
                                    <i class="fas fa-check-circle text-info"></i>
                                </div>
                                <div class="stat-content">
                                    <div class="stat-number" id="inStock">
                                        <i class="fas fa-spinner fa-spin"></i>
                                    </div>
                                    <div class="stat-label">Còn hàng</div>
                                </div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-icon">
                                    <i class="fas fa-exclamation-triangle text-warning"></i>
                                </div>
                                <div class="stat-content">
                                    <div class="stat-number" id="outOfStock">
                                        <i class="fas fa-spinner fa-spin"></i>
                                    </div>
                                    <div class="stat-label">Hết hàng</div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Card hướng dẫn -->
                <div class="card help-card">
                    <div class="card-header">
                        <h5 class="card-title">
                            <i class="fas fa-lightbulb"></i>
                            Lưu ý khi chỉnh sửa
                        </h5>
                    </div>
                    <div class="card-body">
                        <ul class="help-list">
                            <li>
                                <i class="fas fa-info text-info"></i>
                                Thay đổi tên thương hiệu có thể ảnh hưởng đến hiển thị trên website
                            </li>
                            <li>
                                <i class="fas fa-warning text-warning"></i>
                                Không thể xóa thương hiệu nếu còn sản phẩm
                            </li>
                            <li>
                                <i class="fas fa-check text-success"></i>
                                Logo mới sẽ thay thế logo cũ
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/admin_brands.js" asp-append-version="true"></script>
    <script>
        // Load thống kê thương hiệu
        document.addEventListener('DOMContentLoaded', function() {
            loadBrandStats(@Model.BrandID);
        });
    </script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}