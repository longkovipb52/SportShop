@model IEnumerable<SportShop.Models.Category>

@{
    ViewData["Title"] = "Quản lý danh mục";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/admin_categories.css" asp-append-version="true" />
}

<div class="categories-container">
    <!-- Header với nút thêm mới -->
    <div class="page-header">
        <div class="header-content">
            <div class="header-right">
                <a href="@Url.Action("Create")" class="btn btn-primary">
                    <i class="fas fa-plus"></i>
                    Thêm danh mục mới
                </a>
            </div>
        </div>
    </div>

    <!-- Thanh tìm kiếm và bộ lọc -->
    <div class="search-filter-section">
        <div class="categories-search-box">
            <form method="get" asp-action="Index" class="search-form">
                <div class="input-group">
                    <input type="text" name="search" value="@ViewData["CurrentSearch"]" 
                           class="form-control" placeholder="Tìm kiếm theo tên hoặc mô tả...">
                    <button type="submit" class="btn btn-outline-secondary">
                        <i class="fas fa-search"></i>
                    </button>
                    @if (!string.IsNullOrEmpty(ViewData["CurrentSearch"]?.ToString()))
                    {
                        <a href="@Url.Action("Index")" class="btn btn-outline-danger">
                            <i class="fas fa-times"></i>
                        </a>
                    }
                </div>
            </form>
        </div>
    </div>

    <!-- Hidden elements for TempData messages -->
    @if (TempData["SuccessMessage"] != null)
    {
        <span data-tempdata-success="@TempData["SuccessMessage"]" style="display: none;"></span>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <span data-tempdata-error="@TempData["ErrorMessage"]" style="display: none;"></span>
    }

    <!-- Bảng danh sách danh mục -->
    <div class="categories-table-section">
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table categories-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Hình ảnh</th>
                            <th>Tên danh mục</th>
                            <th>Mô tả</th>
                            <th>Số sản phẩm</th>
                            <th>Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var category in Model)
                        {
                            <tr>
                                <td class="category-id">
                                    <span class="id-badge">#@category.CategoryID</span>
                                </td>
                                <td class="category-image">
                                    @if (!string.IsNullOrEmpty(category.ImageURL))
                                    {
                                        <img src="~/upload/category/@category.ImageURL" alt="@category.Name" class="category-img">
                                    }
                                    else
                                    {
                                        <div class="no-image">
                                            <i class="fas fa-image"></i>
                                        </div>
                                    }
                                </td>
                                <td class="category-name">
                                    <strong>@category.Name</strong>
                                </td>
                                <td class="category-description">
                                    @if (!string.IsNullOrEmpty(category.Description))
                                    {
                                        <span class="description-text">@(category.Description.Length > 100 ? category.Description.Substring(0, 100) + "..." : category.Description)</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Chưa có mô tả</span>
                                    }
                                </td>
                                <td class="category-products">
                                    <span class="product-count" data-category-id="@category.CategoryID">
                                        <i class="fas fa-spinner fa-spin"></i>
                                    </span>
                                </td>
                                <td class="category-actions">
                                    <div class="action-buttons">
                                        <a href="@Url.Action("Details", new { id = category.CategoryID })" 
                                           class="btn btn-sm btn-info" title="Xem chi tiết">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a href="@Url.Action("Edit", new { id = category.CategoryID })" 
                                           class="btn btn-sm btn-warning" title="Chỉnh sửa">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <button type="button" class="btn btn-sm btn-danger delete-category-btn" 
                                                data-category-id="@category.CategoryID" 
                                                data-category-name="@category.Name" 
                                                title="Xóa">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <!-- Phân trang -->
            @if ((int)ViewData["TotalPages"] > 1)
            {
                <nav aria-label="Categories pagination" class="pagination-nav">
                    <ul class="pagination justify-content-center">
                        @{
                            var currentPage = (int)ViewData["CurrentPage"];
                            var totalPages = (int)ViewData["TotalPages"];
                            var search = ViewData["CurrentSearch"]?.ToString();
                        }

                        <!-- Previous button -->
                        <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                            <a class="page-link" href="@Url.Action("Index", new { page = currentPage - 1, search = search })">
                                <i class="fas fa-chevron-left"></i>
                            </a>
                        </li>

                        <!-- Page numbers -->
                        @for (var i = Math.Max(1, currentPage - 2); i <= Math.Min(totalPages, currentPage + 2); i++)
                        {
                            <li class="page-item @(i == currentPage ? "active" : "")">
                                <a class="page-link" href="@Url.Action("Index", new { page = i, search = search })">@i</a>
                            </li>
                        }

                        <!-- Next button -->
                        <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                            <a class="page-link" href="@Url.Action("Index", new { page = currentPage + 1, search = search })">
                                <i class="fas fa-chevron-right"></i>
                            </a>
                        </li>
                    </ul>
                </nav>

                <div class="pagination-info">
                    <small class="text-muted">
                        Hiển thị @((currentPage - 1) * (int)ViewData["PageSize"] + 1) - 
                        @(Math.Min(currentPage * (int)ViewData["PageSize"], (int)ViewData["TotalRecords"])) 
                        trong tổng số @ViewData["TotalRecords"] danh mục
                    </small>
                </div>
            }
        }
        else
        {
            <div class="empty-state">
                <div class="empty-icon">
                    <i class="fas fa-tags"></i>
                </div>
                <h3>Chưa có danh mục nào</h3>
                <p>Bắt đầu bằng cách thêm danh mục đầu tiên cho cửa hàng của bạn</p>
                <a href="@Url.Action("Create")" class="btn btn-primary">
                    <i class="fas fa-plus"></i>
                    Thêm danh mục đầu tiên
                </a>
            </div>
        }
    </div>
</div>

<!-- Modal xác nhận xóa -->
<div class="modal fade" id="deleteCategoryModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Xác nhận xóa danh mục</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Bạn có chắc chắn muốn xóa danh mục <strong id="categoryNameToDelete"></strong>?</p>
                <p class="text-warning">
                    <i class="fas fa-exclamation-triangle"></i>
                    Hành động này không thể hoàn tác!
                </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-danger" id="confirmDeleteBtn">Xóa</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Html.AntiForgeryToken()
    <script src="~/js/admin_categories.js" asp-append-version="true"></script>
}